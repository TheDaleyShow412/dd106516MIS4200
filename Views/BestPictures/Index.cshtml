@model IEnumerable<dd106516MIS4200.Models.BestPicture>

@{
    ViewBag.Title = "Best Picture Winners";
}

<h2>Best Picture Results</h2>

<p> This did work at one point and then I changed some things and now it doesn't.</p>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.movieID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.movieTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.eligibleYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.winner)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.movieID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.movieTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.eligibleYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.winner)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.bestPictureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.bestPictureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.bestPictureID })
        </td>
    </tr>
}

</table>
